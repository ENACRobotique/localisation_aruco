#! /usr/bin/env python

import sys
import RequestCalib

if __name__ == '__main__':
    if len(sys.argv) != 2:
        print "mauvaise Utilisation: ajouter seulement le nombre de camera a recalibrer"
        sys.exit(1)
    RequestCalib.calib(int(sys.argv[1]))
